Start by reading 'Agent-starter.md' and get context

we're gonna first make dashboard page of app where user can do refer 'project-overview.md line 70 to 114'

install dependencies if the feature is added which use it and don't always assume dependencies, install it

refer 'ui-context.md' for ui context

for code context refer 'code-standards.md'

also update 'progress-tracker.md' accordingly

completed when:
dashboard UI is done and every function is working.
Don't make dummy thing, everything must be real.
